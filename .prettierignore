node_modules/
package-lock.json

# Template files are validated data artifacts with a strict render and ATS
# contract. The pack validator is their gate, not Prettier; keeping them out of
# Prettier avoids reflow churn and any risk of altering a validated template.
templates/

# release-please rewrites these on each release PR and reserializes JSON in a
# form Prettier would reformat. Ignore them so the release PR never trips
# format:check.
CHANGELOG.md
.release-please-manifest.json
pack.json
